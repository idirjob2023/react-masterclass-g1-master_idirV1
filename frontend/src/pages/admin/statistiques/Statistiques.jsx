import React from "react";
import { Outlet } from "react-router-dom";
const Statistiques = () => {
  return (
    <div>
      Statistiques
      <Outlet />
    </div>
  );
};

export default Statistiques;
